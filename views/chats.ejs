<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Chat - Chats</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-[#f0f2f5]">
    <div class="max-w-md mx-auto bg-white shadow-md h-screen flex flex-col">
        <div class="bg-[#075e54] text-white p-4 flex justify-between items-center">
            <h1 class="text-xl font-semibold">WhatsApp</h1>
            <div>
                <a href="#" class="text-white mr-4"><i class="fas fa-camera"></i></a>
                <a href="#" class="text-white mr-4"><i class="fas fa-search"></i></a>
                <a href="#" class="text-white"><i class="fas fa-ellipsis-v"></i></a>
            </div>
        </div>
        <div class="p-4 flex-grow overflow-y-auto">
            <div class="mb-4 relative">
                <i class="fas fa-search absolute top-3 left-3 text-gray-400"></i>
                <input type="text" id="searchInput" placeholder="Search or start new chat" 
                       class="w-full pl-10 pr-3 py-2 bg-[#f0f2f5] rounded-full focus:outline-none focus:ring-2 focus:ring-[#25D366] focus:border-transparent">
            </div>
            <ul id="userList" class="space-y-2">
                <% users.forEach(user => { %>
                    <li>
                        <a href="/chat/<%= currentUser._id %>/<%= user._id %>" class="flex items-center p-3 hover:bg-[#f0f2f5] rounded-md transition duration-300">
                            <div class="w-12 h-12 bg-[#DFE5E7] rounded-full flex items-center justify-center text-[#075e54] font-semibold mr-3">
                                <%= user.username.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <span class="text-gray-800 font-medium"><%= user.username %></span>
                                <p class="text-gray-500 text-sm">Tap to chat</p>
                            </div>
                        </a>
                    </li>
                <% }); %>
            </ul>
        </div>
        <div class="bg-[#f0f2f5] border-t border-gray-200 p-4 flex justify-around">
            <a href="#" class="text-[#075e54]"><i class="fas fa-comment-alt"></i></a>
            <a href="#" class="text-[#075e54]"><i class="fas fa-users"></i></a>
            <a href="#" class="text-[#075e54]"><i class="fas fa-circle"></i></a>
            <a href="#" class="text-[#075e54]"><i class="fas fa-phone-alt"></i></a>
            <a href="/" class="text-[#075e54]"><i class="fas fa-sign-out-alt"></i></a>
        </div>
    </div>
    <script>
        const searchInput = document.getElementById('searchInput');
        const userList = document.getElementById('userList');
        const users = userList.getElementsByTagName('li');

        searchInput.addEventListener('input', function() {
            const filter = this.value.toLowerCase();
            for (let i = 0; i < users.length; i++) {
                const username = users[i].textContent.toLowerCase();
                users[i].style.display = username.includes(filter) ? "" : "none";
            }
        });
    </script>
</body>
</html>